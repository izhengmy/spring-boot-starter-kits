spring:
  application:
    name: spring-boot-starter-kits

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/app?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=UTF-8
    username: root
    password: root

logging:
  level:
    mybatis-flex-sql: DEBUG
